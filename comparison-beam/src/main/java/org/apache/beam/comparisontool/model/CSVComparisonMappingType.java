package org.apache.beam.comparisontool.model;

/**
 * Enum with the appropriate mapping types (this list will need to 
 * be added to in future. These values just represent a flavour of what might 
 * be in the mapping file)
 * 
 * @author mark.lupine
 *
 */
public enum CSVComparisonMappingType {
	
	STRING, INTEGER, DOUBLE, CURRENCY;
	
}
